# Copy to .env.local to override where the frontend sends API requests.
# Without this, it falls back to http://localhost:4000/api (the local dev server) —
# see src/lib/api.ts. On Vercel, set VITE_API_BASE_URL as a real project environment
# variable pointing at wherever the backend (server/) is actually hosted.
VITE_API_BASE_URL=
